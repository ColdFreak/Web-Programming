javascriptの関数の一つはgetElementsByName, 複数形式に注意


JQueryを使って、checkbox_1というidのチェックボックスをチェックされているかの関数
function submitbox() {
  if ($('#checkbox_1').is(':checked')) {
	  alert("checked");
  } else {
	alert("not checked");
  }
}


window opener を使って、for文使って, 親windowの関数を連続呼び出すことができる。
そして、myFunc()が親window中のオブジェクトをいじることができる。
window.opener.myFunc();
親windowのmyFunc関数に$("p").append("sample text");など
のものによって、新しいオブジェクトを生成することができる。



