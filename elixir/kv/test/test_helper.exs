# Mix generate test/test_helper.exs which is responsible for setting up the 
# test framework. This file will be automatically required by Mix every time 
# before we run tests with '$ mix test'
ExUnit.start()
